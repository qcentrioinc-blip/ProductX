{
  "framework": "vite",
  "buildCommand": "cd productx && npm install && npm run build",
  "outputDirectory": "productx/dist",
  "installCommand": "cd productx && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}